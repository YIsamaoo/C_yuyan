#include <stdio.h>
#include <stdbool.h>
#include<windows.h>
#include<time.h>
#include<stdlib.h>


int main()
{
	int i=0;
	int count = 0;

	srand((unsigned)time(NULL)); int arandom = rand()%1000;
	do {
		scanf_s("%d", &i); count++;
		if (i > arandom) {
			printf("dale");
		}
		else if (i < arandom) {
			printf("xiaole");
		}

	} while (i != arandom);

		printf("duile");
	return 0;
}
